image: gradle:alpine
variables:
  GRADLE_OPTS: "-Dorg.gradle.daemon=false"
before_script:
  - set GRADLE_USER_HOME=`pwd`/.gradle
stages:
  - sonar
  - validate
  - build
  - test
build:
  stage: build
  script: gradle build
  cache:
    key: "$CI_COMMIT_REF_NAME"
    policy: push
    paths:
      - build
      - .gradle   
checkstyle:
  stage: validate
  script:
    -  java -jar checkstyle-8.36.2-all.jar -c google_checks.xml .
  allow_failure: true
staticanalysis:
  stage: validate
  script:
    - gradle validate
  allow_failure: true
sonarqube-web:
  stage: sonar
  script:
    - cd webapplication/Sticks-n-pucks
    - gradle sonarqube -Dsonar.projectKey=sticksnpucks -Dsonar.host.url=http://localhost:9000 -Dsonar.login=c14d731517eb24eef16d09d41d5f9791df9791af
  allow_failure: true
sonarqube-api:
  stage: sonar
  script:
    - cd api/sticks-pucks
    - gradle sonarqube   -Dsonar.projectKey=SticksAPI -Dsonar.host.url=http://localhost:9000   -Dsonar.login=78f0ee16ea081c8f7c39ac891d7b929467c53d32
  allow_failure: true
testapi:
  stage: test
  script: 
    - cd api/sticks-pucks
    - gradle clean test
  cache:
    key: "$CI_COMMIT_REF_NAME"
    policy: pull
    paths:
      - build
      - .gradle
testweb:
  stage: test
  script: 
    - cd webapplication/Sticks-n-pucks
    - gradle clean test
  cache:
    key: "$CI_COMMIT_REF_NAME"
    policy: pull
    paths:
      - build
      - .gradle
